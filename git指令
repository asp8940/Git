git add . 
git commit -m 'txt'
git push origin 分支名稱

--------

git branch name 新建一個新分支
git checkout name 切到該分支  

--------

git add .
git commit -m 'initial commit'
git remote rm origin
git remote add origin git@ip:/path
git push origin master

-------
git reset commit代號    回到某個版本文件
git remote show origin  顯示遠端所有分支
git push -f             強制push
git branch              所有分支
git branch -m old new   重新命名分支
